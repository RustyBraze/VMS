<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VMS Installation</title>
    <link rel="stylesheet" href="{{ asset('assets/css/bootstrap.min.css') }}">
    <style>
        body { background-color: #f8f9fa; }
        .wizard-container { max-width: 600px; margin: 50px auto; padding: 20px; background: white; border-radius: 8px; box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1); }
        .wizard-header { font-size: 24px; margin-bottom: 20px; }
        .wizard-footer { margin-top: 20px; }
    </style>
</head>
<body>
<div class="container">
    <div class="wizard-container">
        <h2 class="wizard-header">{{ title }}</h2>
        <div class="progress">
            <div class="progress-bar" role="progressbar" style="width: {{ progress }}%;" aria-valuenow="{{ progress }}" aria-valuemin="0" aria-valuemax="100"></div>
        </div>
        <hr>
        {% block content %}{% endblock %}
    </div>
</div>
</body>
</html>
